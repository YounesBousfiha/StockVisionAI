spring:
  profiles:
    active: dev
  application:
    name: stockvisionai
  cloud:
    vault:
      authentication: TOKEN
      token: ${VAULT_TOKEN:hvs.nZCiJCqPE37J254yytaHqEaa}
      scheme: http
      host: ${VAULT_HOST:localhost}
      port: 8200
      kv:
        enabled: true
        backend: secret
        application-name: stockvisionai

  config:
    import: "vault://"

# JWT configuration - values loaded from Vault (secret/stockvisionai)
# Vault provides: jwt.secret-key, spring.data.mongodb.uri, spring.data.redis.host/port
jwt:
  access-token:
    expiration: 900000
  refresh-token:
    expiration: 604800000

spring.ai:
  deepseek:
    api-key: sk-458170e762304c7cbf4e13f018a4f1f5
    base-url: https://api.deepseek.com/chat/completions
    model: deepseek-chat

    chat:
      enabled: true
